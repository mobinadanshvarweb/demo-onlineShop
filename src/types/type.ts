export type NavbarItem = {
  subMenu: [];
};
export type SubMenuItem = {
  slug: string;
  title: string;
};

export type Product = {
  price: number;
  name: string;
  _id: string;
  category: string;
  image: string;
};
